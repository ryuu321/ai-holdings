【SharoText / KenText — Supabase feedbackテーブル追加SQL】

Supabase Dashboard → SQL Editor で実行してください。

-- SharoText
CREATE TABLE sharotext_feedback (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  email text,
  category text,
  rating text NOT NULL,
  regen_count integer DEFAULT 0,
  reasons text,
  created_at timestamptz DEFAULT now()
);
ALTER TABLE sharotext_feedback ENABLE ROW LEVEL SECURITY;
CREATE POLICY "anon_all" ON sharotext_feedback FOR ALL TO anon USING (true) WITH CHECK (true);

-- KenText（同じSupabaseプロジェクトを使う場合）
CREATE TABLE kensetsu_feedback (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  email text,
  category text,
  rating text NOT NULL,
  regen_count integer DEFAULT 0,
  reasons text,
  created_at timestamptz DEFAULT now()
);
ALTER TABLE kensetsu_feedback ENABLE ROW LEVEL SECURITY;
CREATE POLICY "anon_all" ON kensetsu_feedback FOR ALL TO anon USING (true) WITH CHECK (true);
