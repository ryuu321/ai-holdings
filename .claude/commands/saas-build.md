# /saas-build — SaaS開発フレームワーク

SaaSプロダクトを企画から運用まで、品質を落とさずに遂行するための標準プロセス。
引数なしで呼ぶと現在プロジェクトの進捗チェック。引数にプロジェクト名で新規起動。

---

## フェーズ一覧

```
Phase 0:   企画草案
Phase 1:   要件定義
Phase 2:   技術設計
Phase 2.5: 法的・セキュリティ設計  ← スキップ禁止
Phase 3:   テスト設計（実装前）
Phase 4:   実装
Phase 5:   テスト実行・修正
Phase 6:   エラーハンドリング
Phase 7:   CI設定
Phase 8:   デプロイ（ステージング）
Phase 9:   受け入れテスト（UAT）
Phase 10:  本番デプロイ
Phase 11:  運用・モニタリング
```

---

## Phase 0: 企画草案

**目的**: 作る価値があるか判断する。コードは1行も書かない。

---

### 0-A: 事業設計原則（全プロジェクト共通・変えない）

AI Holdingsの事業はすべてこの基準を満たすこと:

| 基準 | 内容 |
|------|------|
| 稼げる | 単価 × 顧客数で月100万以上の算数が成り立つ |
| 捕まらない | 法的グレーゾーンなし・専門家責任を負わない設計 |
| スケール可能 | 顧客が増えてもコストが線形に増えない |
| 解約されにくい | データロック × 習慣化で構造的にスイッチングコストを作る |
| 責任が軽い | 「ツール提供者」止まり。判断はユーザーが行う設計 |

**意図的に捨てた基準**: 「競合に専門性で抜かれない」  
→ 専門性を深めると「専門家」として見られ訴訟リスクが上がる。代わりにデータロックで優位性を維持する。

---

### 0-B: 解約防止の設計原則（データロック × 習慣化）

**コア戦略**: データロック × 習慣化の二重ロック

```
習慣化: 毎日・毎週使われる導線 → 使わないと業務が回らない状態
データロック: 使うたびに顧客固有データが蓄積 → 乗り換えると全部消える
         ↓ 組み合わせると
スイッチングコスト = 習慣の破壊 + データの消失（二重コスト）
```

**実装ロードマップ（順番に仕込む）:**

| フェーズ | 手法 | 実装タイミング |
|---------|------|------------|
| 今すぐ | データロック（履歴保存） | MVP設計段階から必須 |
| 今すぐ | 習慣化（定期ユースケース設計） | MVP設計段階から必須 |
| ユーザーが増えたら | ワークフロー統合（既存ツールAPI連携） | PMF確認後 |
| 製品が増えたら | マルチプロダクト（製品間データ共有） | 2本目以降 |
| 規模が出たら | ネットワーク効果（集合データ活用） | 数百ユーザー以降 |

**チェックリスト（設計時に必ず確認）:**
- [ ] ユーザーが毎日or毎週使う理由があるか（習慣化）
- [ ] 使うたびにユーザー固有のデータが増えるか（データロック）
- [ ] 1年後、乗り換えたら何を失うかが明確か（スイッチングコスト）
- [ ] 乗り換えコストが「お金」ではなく「データ・習慣の喪失」になっているか

---

### 0-C: 市場評価

確認する項目:
- [ ] **誰が払うか** — 具体的な職種・業種（「中小企業」はNG、「工務店の見積担当者」がOK）
- [ ] **何に困っているか** — 現在の解決手段と、その手段の具体的な不満
- [ ] **なぜAIで解決できるか** — 既存SaaSではなくAIが必要な理由
- [ ] **月いくら払うか** — 代替手段のコスト（時間×時給）と比較して適正単価を出す
- [ ] **¥5M到達の数字** — 単価 × 必要顧客数。現実的か？
- [ ] **TAM** — 対象市場の全体規模（登録事業者数 × 単価）

**GO基準**: 0-A・0-Bのチェックリストが埋まり、0-Cの数字が出たらPhase 1へ。
**STOP基準**: 「誰が払うか」「月いくら」が曖昧、またはデータロック・習慣化の設計が見えないなら企画を再考する。

---

## Phase 1: 要件定義

**目的**: 「完成」の定義を決める。後から変えない。

成果物:
- **ユーザーストーリー**: 「〇〇として、△△したい。なぜなら××だから。」の形式で3〜5本
- **受け入れ基準**: 各ストーリーに対して「これができればOK」という具体的条件
- **スコープ外リスト**: 今回作らないものを明示する（機能追加の誘惑を断ち切る）
- **制約条件**: 予算・技術スタック・納期・無料縛り など

**完了基準**: 受け入れ基準を見れば「テストが書けるか」判断できる状態。

---

## Phase 2: 技術設計

**目的**: どう作るかを決める。過剰設計しない。

決めること:
- [ ] **技術スタック** — 既存プロジェクトと揃える（新技術の導入は理由を明記）
- [ ] **ファイル構成** — `app.py` / `{domain}.py` / `tests/` の最小構成
- [ ] **外部依存** — APIキー管理方法・.envと.gitignoreの設計
- [ ] **データフロー** — 入力→処理→出力を1枚の図（テキストでOK）
- [ ] **エラーパス** — 正常系だけでなく、何が壊れ得るかをリストアップ

**原則**: 3行で書けるなら3行で書く。抽象化は3回同じコードを書いてから。

---

## Phase 2.5: 法的・セキュリティ設計（**スキップ禁止**）

**目的**: 1つのミスで事業が終わるリスクを、コードを1行も書く前に潰す。
**完了基準**: 全項目にチェックが入り、法的ページが公開URLで目視確認できる。

---

### A. 特定商取引法に基づく表記（有償取引がある場合・必須）

- [ ] 販売事業者の氏名（例: 真柄 龍聖）
- [ ] 所在地 — **バーチャルオフィス住所を実記載する**。「請求があり次第開示」は住所を持っていない個人だけの例外。住所がある場合は必ず記載すること
- [ ] 連絡先メール
- [ ] 販売価格（具体的な金額 or 「お問い合わせください」）
- [ ] 支払方法（銀行振込の場合は「振込先は契約時にご案内」でOK）
- [ ] 提供時期（「入金確認後〇営業日以内」）
- [ ] 返品・キャンセルポリシー（デジタルコンテンツは「アクセスコード発行後は返金不可」でOK）

→ `saas-stack.md` の LP HTML テンプレートに完成品あり。コピーして住所・価格だけ書き換える

---

### B. プライバシーポリシー（メールアドレス等を取得する場合・必須）

- [ ] 収集する情報を明示（メールアドレス・入力データ等）
- [ ] 収集しない情報を明示（「物件情報はサーバーに保存しません」等）
- [ ] 外部AIへの送信がある場合はその旨を記載（Gemini等にプロンプトとして送信されることを開示）
- [ ] 利用目的（回答・サービス改善・契約手続きのみ）
- [ ] 第三者提供しない旨
- [ ] 開示・訂正・削除の請求先（メールアドレス）

→ `saas-stack.md` の LP HTML テンプレートに完成品あり。コピーしてメールと日付だけ書き換える

---

### C. 特定電子メール法（コールドメール送信をする場合・必須）

- [ ] 送信者の氏名をメール本文に明記
- [ ] 所在地（バーチャルオフィス住所）をメール本文に明記
- [ ] 配信停止の申し出先をメール本文に明記（「このメールに『配信停止』とご返信ください」）
- [ ] `opt_out.csv` を作成し、送信前に毎回照合する（`send_emails.py` は実装済み）
- [ ] 配信停止の申し出から**2営業日以内**に送信停止する — 申し出があったら即日 `python send_emails.py --add-opt-out メアド` を実行
- [ ] 送信時間帯を JST 9:00〜18:00 に制限する
- [ ] 1日の送信件数を 30 件以内に制限する

→ `saas-dev/projects/fudosan-copy/outreach/` が参照実装。`shared/gtm/` に共通化済み

---

### D. 業法確認（対象業界ごとに確認・スキップ禁止）

| 業界 | 確認すること |
|------|------------|
| 不動産 | 景品表示法: AI出力に「最良・最高・一番」等の最大級表現がないか確認 |
| 医療・健康 | 薬機法: 効果・効能の断定表現は不可。「〇〇に効く」はアウト |
| 金融・投資 | 金商法: 「必ず儲かる」等の断定的判断の提供は不可 |
| BtoB全般 | ツールを提供するだけでも、利用者が業法違反しやすい設計になっていないか確認 |

---

### E. セキュリティ

- [ ] APIキーはコードにハードコードしない（`.env` or Streamlit Secrets）
- [ ] `.env` と `secrets.toml` が `.gitignore` に含まれている
- [ ] フリーテキスト入力にプロンプトインジェクション対策がある（`saas-stack.md` のパターン参照）
- [ ] 1セッションあたりの生成回数上限がある（APIクォータ保護）
- [ ] AI出力の直後に「生成内容は必ず確認してください」のウォーニングを表示している

---

## Phase 3: テスト設計（実装前）

**目的**: 何をテストするか先に決める。テストが仕様書になる。

テストの種類と優先度:
1. **単体テスト（必須）** — 純粋関数・バリデーション・変換ロジック
2. **エラーハンドリングテスト（必須）** — API障害・不正入力・クォータ超過
3. **統合テスト（推奨）** — 外部APIはモックせず実際に叩く（ただしCI用のフラグで制御）
4. **E2Eテスト（任意）** — UIがあるなら最低1本のハッピーパス

**禁止事項**:
- テストのためだけに内部実装を変える（テストが実装に依存するのはNG）
- モックだらけのテスト（実際に動かないのに「パス」するテストは無価値）
- **「実装が動く」ことをテストする（= 実装のコピー）** — ビジネス要件をテストすること

---

### GTMパイプライン 必須テスト仕様（コールドメール送信系）

> **教訓 (2026-05-19)**: `_clean_company_name()` のテストがゼロだった。
> 29件送信中4件以上がブログタイトル・名簿業者・ターゲット外企業に誤送信。
> 関数が「動く」テストはあったが「正しい企業名だけ抽出する」テストがなかった。

**GTMパイプラインを作るたびに必須で書くテスト:**

#### A. データクリーニング関数（`_clean_company_name` 相当）

| テストクラス | 何をテストするか |
|------------|----------------|
| `TestRejectsBlogTitles` | ブログ記事タイトルは必ず `""` を返すこと（実際の誤送信入力を使う） |
| `TestRejectsBlacklist` | 名簿業者・営業リスト系は `""` を返すこと |
| `TestExtractsCorrectly` | 「お問い合わせ \| 〇〇株式会社」→ 法人名だけ抽出できること |
| `TestEdgeCases` | 空文字・法人格なし → `""`、フォールバック（`raw[:20]`等）が存在しないこと |

**具体的なルール:**
- テスト入力は「実際に失敗した入力」を優先。架空のきれいな入力だけでは不十分
- `assert result == ""` のテストと `assert result == "正しい会社名"` のテストを両方書く
- `raw_name[:N]` のような切り捨てフォールバックが存在しないことを明示的に確認する

#### B. リード除外ロジック（qualify / safety_check 相当）

```python
# このパターンのテストが必須
def test_blog_title_skipped_in_pipeline():
    # _clean_company_name が "" を返す入力はパイプラインでSKIPされること
    # generate_emails.main() が該当リードをdraft.csvに書かないことを確認
    ...

def test_safety_check_blocks_blog_company_name():
    # _check_safety() がブログ会社名を含むドラフトでFalseを返すこと
    ...
```

#### C. 重複除外（`load_existing_emails` 相当）
- 送信済みアドレスが再度ドラフトに入らないことを確認
- draft.csv と sent_log.csv の union が取れることを確認

**完了基準**: 
1. テストファイルの骨格（クラス名・テスト名）が書けた状態
2. GTMパイプラインの場合: 上記A〜Cのクラスがすべて存在すること
3. `pytest -v` でグリーンになること（実装前はskipで可、実装後はすべてpass必須）

---

## Phase 4: 実装

**目的**: テスト設計に従ってコードを書く。

守るルール:
- [ ] **既存コードを読んでから書く** — 重複・競合の防止
- [ ] **APIキーはハードコードしない** — 必ず環境変数
- [ ] **コメントは「なぜ」だけ** — 「何をしているか」は変数名で伝える
- [ ] **1関数1責任** — 生成・検証・整形は別関数に分ける
- [ ] **エラーパスを実装に含める** — Phase 2で洗い出した障害ケースを必ず実装

**完了基準**: `python app.py` または `streamlit run app.py` でローカル動作する。

---

## Phase 5: テスト実行・修正

**目的**: Phase 3で設計したテストをすべてパスさせる。

手順:
1. `pytest tests/ -v` を実行
2. 失敗したテストは実装を修正する（テストを緩めない）
3. カバレッジが薄い箇所があれば追加テストを書く
4. 全テストパスを確認してからコミット

**完了基準**: `pytest tests/ -v` で全テストグリーン。

---

## Phase 6: エラーハンドリング

**目的**: 本番で壊れたときユーザーに意味のあるメッセージを返す。

チェックリスト:
- [ ] **外部API障害** — 429/500/タイムアウトを個別に捕捉して `ok: False` で返す
- [ ] **クォータ超過** — リトライせず即座に返す（無駄なリトライは問題を悪化させる）
- [ ] **ネットワーク断** — リトライ回数上限を設ける（無限ループ禁止）
- [ ] **不正入力** — ユーザー入力は信頼しない。境界値を確認する
- [ ] **エラーメッセージ** — 技術的な例外をそのまま見せない。ユーザーが次にやるべきことを示す

**完了基準**: エラーハンドリングのテストがすべてパスする。

---

## Phase 7: CI設定

**目的**: pushのたびに自動でテストが走る状態にする。

`.github/workflows/{project}-test.yml` の最小構成:
- トリガー: `push` / `pull_request` (対象パスのみ)
- Python環境のセットアップ
- `pip install -r requirements.txt pytest`
- `pytest tests/ -v --tb=short`
- APIキーはダミー値を `env:` に設定（テストはモック済みのため実APIは不要）

**完了基準**: GitHubのActionsタブでグリーンになる。

---

## Phase 8: デプロイ（ステージング）

**目的**: 実際のURLで動作を確認する。本番ではない。

Streamlit Cloudの場合:
1. `streamlit.io/cloud` でリポジトリを接続
2. `secrets.toml` にAPIキーを設定（GitHubには入れない）
3. アプリのURLを取得
4. ハッピーパスを1件手動で動かす

**完了基準**: 公開URLで実際に生成が動く。

---

## Phase 9: 受け入れテスト（UAT）

**目的**: Phase 1の受け入れ基準を実際に満たしているか確認する。

手順:
1. Phase 1の受け入れ基準リストを開く
2. 1件ずつ実際に操作して確認する
3. NGがあれば実装に戻る（「だいたい動く」は完了ではない）

**完了基準**: 受け入れ基準の全項目が実際の操作で確認できた。

---

## Phase 10: 本番デプロイ

**目的**: 実ユーザーに届ける状態にする。

チェックリスト:
- [ ] ステージングと本番で同じ挙動
- [ ] エラー時のメッセージが非技術者にも伝わる
- [ ] 料金・プラン・トライアル期間が明示されている（無料トライアルの範囲を決める）
- [ ] 問い合わせ先（メール等）が表示されている

---

## Phase 11: 運用・モニタリング

**目的**: 壊れたときに誰が・何を見て気づくか決める。

最低限やること:
- [ ] **エラーログ** — 生成失敗・APIエラーを記録する仕組み
- [ ] **週次確認** — APIクォータ残量・課金状況を確認する（週10分）
- [ ] **フィードバック導線** — ユーザーが不満を伝えられる場所（メール/フォーム）
- [ ] **解約防止** — 30日使っていないユーザーへのリマインド（将来的に）

---

## 現在地チェック（引数なしで呼ばれた場合）

1. `saas-dev/projects/` 以下のアクティブプロジェクトを確認
2. 各プロジェクトのファイル構成からフェーズを推定
3. 「今どこにいるか」と「次のアクション」を表形式で報告

---

## 絶対ルール

- **フェーズを飛ばさない** — Phase 3（テスト設計）をスキップした実装は受け入れない
- **「だいたい動く」は完了ではない** — 受け入れ基準を満たすまで次に進まない
- **テストを緩めてパスさせない** — 実装を直す。テストの期待値を下げない
- **コードより先に仕様を変えない** — 途中で要件が変わったらPhase 1に戻る

---

## 実戦知見ライブラリ（踏んだ地雷集）

開発中に実際に踏んだ問題とその解決策。次のプロジェクトで同じ穴に落ちないために。

---

### Streamlit Cloud

**① `st.page_link()` は Cloud でほぼ使えない**
- Cloud の CWD はリポジトリルート。アプリがサブディレクトリにあると `pages/` の解決に失敗して `KeyError` が出る
- 日本語ファイル名を ASCII にリネームしても治らない（内部レジストリの問題）
- **解決策**: 法的ページ・補足情報は `st.expander()` でメインページに折り畳んで入れる。マルチページはローカル専用と思え

**② `st.secrets` の TOML はスマートクォート厳禁**
- コピペで入力すると `"` (curly quote) が混入して「Invalid format: please enter valid TOML」エラー
- **解決策**: Secrets 入力欄には必ずキーボードで直接入力する（貼り付けた後に引用符を打ち直す）

**④ ボタン操作後に生成結果が消える**
- Streamlit はボタンを押すたびにスクリプト全体を再実行する
- `if submitted:` ブロック内に結果表示を書くと、次の操作（フィードバックボタン等）で消える
- **解決策**: 生成結果を `st.session_state` に保存し、表示ブロックを `if submitted:` の外に出す
```python
# NG: submitted=False になると消える
if submitted:
    result = generate(...)
    st.code(result["body"])  # ← ボタン操作で消える

# OK: session_state から常に描画
if submitted:
    st.session_state.last_result = generate(...)

if st.session_state.get("last_result"):
    st.code(st.session_state.last_result["body"])  # ← 消えない
```

**③ ローカルと Cloud の二重シークレット対応パターン**
```python
if "GEMINI_API_KEY" in st.secrets:
    os.environ["GEMINI_API_KEY"] = st.secrets["GEMINI_API_KEY"]
else:
    from dotenv import load_dotenv
    load_dotenv(dotenv_path=os.path.join(os.path.dirname(__file__), "../../.env"))
```
- `st.secrets.get("KEY", "")` でオプショナルなシークレットを安全に読む

---

### Google Gemini API

**① `google.generativeai` は廃止済み**
- `pip install google-generativeai` → 使えるが deprecated。警告が出る
- **正しい SDK**: `pip install google-genai`
```python
from google import genai
client = genai.Client(api_key=os.environ["GEMINI_API_KEY"])
response = client.models.generate_content(model=MODEL, contents=prompt)
raw = response.text.strip()
```

**② モデル名はバージョン固定より `latest` を使う**
- `gemini-2.0-flash` などバージョン固定はクォータが先に枯れる
- **推奨**: `MODEL = "gemini-flash-latest"` （自動で最新 Flash を使う）

**③ クォータエラーはリトライしない**
```python
if "429" in err_str or "RESOURCE_EXHAUSTED" in err_str or "quota" in err_str.lower():
    return {"ok": False, "error": "APIの利用上限に達しました...", "error_type": "quota"}
```
- クォータ超過でリトライすると問題を悪化させるだけ。即座に返してユーザーに待ってもらう
- それ以外のエラーは最大3回リトライ

---

### AI 出力品質の担保

**① 出力長は必ず下限を設ける**
- 指示しないと AI は短い出力（30文字など）を返してパスしてしまう
- **パターン**: `body_min = max(100, body_max // 2)` で下限を決めてリトライ
```python
if len(body) < body_min:
    continue  # リトライ
```

**② 補足情報は「必ず織り込む」と明示する**
- `extra` を物件情報欄に入れるだけでは AI が無視することがある
- **解決策**: プロンプトのルール欄に専用ルールを追加する
```python
extra_rule = f"8. 補足情報「{extra}」は必ず物件説明文の本文に具体的に織り込む。" if extra else ""
```

**③ 出力パース → 品質チェック → 長さチェックの順で行う**
```
parse (CATCH:/BODY:) → quality_ok() → len >= body_min → return
↑ どこかで落ちたら continue（最大3回）
```

**④ ブラックリストパターンで低品質出力を弾く**
```python
_BLACKLIST_PATTERNS = [
    r"[ぁ-ん][a-z]",                     # 造語（「ひstyle」「なlife」等）
    r"(快適性|快適さ|快適).{0,5}(快適)", # 重複表現
    r"★+",                               # 記号乱用
    r"(満足性|満足度).{0,5}(満足性|満足度)",
]
```
> **落とし穴**: `[A-Za-z]`（大文字含む）にすると「の**L**DK」「な**D**K」など正常な不動産表現を全ブロックして3回リトライしても全部落ちる。**大文字LDK/DKは正常な業界用語**。小文字のみ `[a-z]` で「ひstyle」等の造語だけを弾くこと。

---

### プロンプトインジェクション対策

フリーテキスト入力は必ずプロンプト構造を壊すパターンを検出・拒否する。

```python
_INJECTION_PATTERNS = [
    r"CATCH\s*:",
    r"BODY\s*:",
    r"【出力",
    r"【物件情報】",
    r"ignore\s+previous",
    r"前の指示を無視",
    r"system\s*:",
    r"---+",
]
```

入力サニタイズも必須:
- 改行・タブ → スペースに正規化（プロンプト構造の破壊を防ぐ）
- 連続スペース → 1つに正規化
- 長さ上限を設けてプロンプト膨張を防ぐ

---

### 自動改善パイプラインの設計

**① ドメイン知識は生成ロジックと分離する**
- 自動改善スクリプトが `prompt.py` を直接書き換えると壊れやすい
- **パターン**: `domain_knowledge.py` にターゲット訴求・ルールを分離し、改善スクリプトはそこだけ更新する
- `prompt.py` は `from domain_knowledge import ...` でインポートする

**② フィードバック収集は Google Sheets 公開 CSV が最安**
- Google Form → 自動でスプレッドシートに記録
- スプレッドシートを「ウェブに公開（CSV）」→ 認証不要で `urllib.request` で読める
- CSV URL 形式: `https://docs.google.com/spreadsheets/d/{ID}/pub?gid={GID}&single=true&output=csv`

**③ コピペ製品のフィードバック設計**
- コピペで使う製品は成約率・使用率の追跡が不可能
- **最も正直なシグナル**: 再生成回数（`st.session_state.request_count`）— 満足したら再生成しない
- 自己申告の 👍/👎 はあてにしすぎない。再生成回数と組み合わせて分析する

**④ バージョン管理パターン**
```python
PROMPT_VERSION = "v1.1"  # domain_knowledge.py で管理
# 改善スクリプトが自動インクリメント: v1.1 → v1.2
```
- `prompt.py` の `PROMPT_VERSION` は廃止し、`domain_knowledge.py` から import する

---

### テスト設計の落とし穴

**① テストデータの長さは `body_min` を超えること**
- `body_min = max(100, body_max // 2)` なので `body_max=400` なら `body_min=200`
- テストの `LONG_BODY` を `body_max + 1` だけにすると `body_min` チェックで落ちる
- **正解**: `LONG_BODY = "あ" * (body_max // 2 + 1)` のように `body_min` を超える長さにする

**② モックの戻り値に `body_min` を意識する**
```python
# NG: 短すぎてリトライに入る
mock_response.text = "CATCH: テスト\nBODY: 短い"

# OK: body_min (200文字) を超える長さにする
LONG_BODY = "あ" * 201
mock_response.text = f"CATCH: テスト\nBODY: {LONG_BODY}"
```

**③ 「実装が動く」テストと「ビジネス要件を満たす」テストを区別する（最重要）**

> **根本原因 (2026-05-19 誤送信)**: `_clean_company_name()` のテストがゼロだった。
> 関数は「動く」ことが確認できていたが、「ブログタイトルを拒否する」「会社名だけ抽出する」という
> ビジネス要件は一度もテストされていなかった。

| 悪いテスト例（実装のコピー） | 良いテスト例（ビジネス要件） |
|---------------------------|--------------------------|
| `assert type(result) == str` | `assert _clean_company_name("不動産会社にメールで〜のコツ!") == ""` |
| `assert len(result) > 0` | `assert _clean_company_name("お問い合わせ \| 東京土地開発株式会社") == "東京土地開発株式会社"` |
| `assert func() is not None` | `assert _clean_company_name("営業リスト販売サービス") == ""` |

**ルール**: データ処理関数のテストは必ず「実際に失敗した入力」か「本番で失敗しうる入力」を使う。
きれいな架空のテストデータだけでは本番のノイズを再現できない。

---

## Phase 12〜16: GTM（Go-to-Market）フェーズ

> Phase 11 で「作って動かす」まで完了。ここからは「売る」フェーズ。
> 実装は `shared/gtm/` に共通化されている。`shared/gtm/README.md` を参照。

```
Phase 12: GTM戦略設計
Phase 13: リード収集・品質管理
Phase 14: メッセージング・シーケンス設計
Phase 15: 送信・追跡・ファネル管理
Phase 16: PDCAループ
```

---

### Phase 12: GTM戦略設計

**目的**: 「誰に・何を・どう届けるか」を設計する。Phase 0の「誰が払うか」より具体的。

成果物:
- `shared/gtm/config/{project}.json` — ICP・チャネル・スコアリングルール
- ICP定義: 「不動産仲介会社の物件担当」レベルの具体性（「中小企業」はNG）
- チャネル選定: 無課金縛り内で週何件アプローチできるか数値で定義

**config.json 必須フィールド確認:**
- [ ] `auto_approve_threshold` が **70以上**（50は低すぎる。FudoText初回誤送信の原因）
- [ ] `exclude_keywords` にターゲット外業種が網羅されているか（工務店・ビル管理・名簿業者・求人）
- [ ] `sender_address` フィールドが入っているか（特定電子メール法）
- [ ] `bad_domain_suffixes` に gmail.com / yahoo.co.jp が入っているか

**完了基準**: `config/{project}.json` が作成され、上記4項目にチェックが入っている。

---

### Phase 13: リード収集・品質管理

**目的**: 「送って恥ずかしくない相手」だけをリストに入れる。量より質。

**ステップ:**

```powershell
# 1. 収集
python saas-dev/projects/{name}/outreach/fetch_leads.py

# 2. スコアリング
python shared/gtm/leads/qualify_leads.py --project {name} --input leads.csv

# 3. 【必須】目視確認（スキップ禁止）
python shared/gtm/outreach/generate_emails.py --project {name} --dry-run
```

**dry-run 後の目視チェックリスト（全件確認）:**
- [ ] 会社名が「株式会社〇〇」「有限会社〇〇」など法人名になっているか
- [ ] 「お問い合わせ |」「〜のコツ」「〜の選び方」「名簿」などブログタイトルが混入していないか
- [ ] メールアドレスのドメインが gmail.com / yahoo.co.jp になっていないか
- [ ] 工務店・ビル管理・清掃業・名簿業者が含まれていないか
- [ ] `personalized=False`（フォールバック）の件が20%以下か

**NGが1件でもあれば送信禁止**。収集・スコアリングに戻って原因を直す。

**完了基準**: 上記チェックリストを全クリアした `leads_approved.csv` が最低20件ある。

> 教訓（FudoText 2026-05-19）: qualify をスキップして `generate_emails.py` を直叩きした結果、名簿業者・工務店・ブログタイトルが混入し29件中4件以上を誤送信。代表者の本名で送っているため信用リスクあり。**パイプライン（pipeline.py）経由以外の送信は絶対禁止。**

---

### Phase 14: メッセージング・シーケンス設計

**目的**: 1通目で断られても2通目・3通目で返信をもらえる設計にする。

手順:
1. `shared/gtm/outreach/templates/` のテンプレートを製品に合わせて編集
2. 編集後に `--preview N` で実際の本文を3件以上読む（自分が受け取ったら違和感がないか）
3. `send_emails.py --test-to 自分のメアド` で実際に受信して確認

**本文チェックリスト（テスト送信後に自分の受信箱で確認）:**
- [ ] 冒頭の会社名が正しい法人名になっているか
- [ ] パーソナライズ文が自然な日本語か（不自然な造語・英字混じりがないか）
- [ ] 署名に氏名・住所・メールが入っているか
- [ ] 配信停止の案内文があるか
- [ ] スパムフォルダに入っていないか（入っていたら件名・送信ドメインを見直す）

**完了基準**: 自分宛テスト送信を受信し、上記チェックリストを全クリア。

---

### Phase 15: 送信・追跡・ファネル管理

**目的**: 送った後に何が起きているかを把握する。「送りっぱなし」を終わらせる。

**初回送信は必ず5件から:**
```powershell
python send_emails.py --limit 5   # まず5件
# → Gmailの送信済みフォルダで本文を目視確認
# → 問題なければ残りを送る
python send_emails.py --limit 25  # 残り25件
```

通常運用:
- `send_emails.py --limit 30`（最大30件/日・平日のみ）
- 返信があったら当日中に対応（口座案内 or opt-out登録）
- `check_replies.py` はGitHub Actionsで1日3回自動実行済み

**完了基準**: 初回5件の本文を送信済みフォルダで目視確認済み。

---

### Phase 16: PDCAループ

**目的**: データに基づいてメッセージング・ターゲット・チャネルを改善する。

**KPI（毎週月曜に確認）:**
| 指標 | 目標 | アクション |
|------|------|-----------|
| 返信率 | ≥ 2% | 未満なら件名・冒頭文を変える |
| 承認率（qualify通過率） | ≥ 40% | 未満なら検索クエリを見直す |
| パーソナライズ成功率 | ≥ 80% | 未満ならGemini 429対策 |

週次手順（5分）:
1. `check_replies.py` の直近ログを確認（Telegramでも通知済み）
2. 返信率 < 2% が2週連続 → 件名A/Bテスト開始
3. 返信率 ≥ 5% → 週30件→50件に増量
4. 改善内容を `config/{project}.json` の `email_template` に反映

**完了基準**: 2週間以上データが蓄積し、KPIが数値で確認できる。
